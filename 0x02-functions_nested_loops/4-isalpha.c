#include "main.h"
/**
 *_isalpha - checking whther a character is alphabet
 *@c: Character to check for
 *
 * Return: 0 or 1
 */
int _isalpha(int c)
{
	if (_isalpha(c) == 0)
		return (0);
	else
		return (1);
}
