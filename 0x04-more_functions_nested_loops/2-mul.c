#include "main.h"
/**
 * mul - function for multiplying numbers
 * @a: First integer to multiply
 * @b: Second integer to multiply
 * Return: Always return a 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (0);
}
