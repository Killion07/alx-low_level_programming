#include "main.h"
/**
 * _abs - Function for finding absolute number
 * @n: The absolute number we are returning
 *
 * Return: The absolute number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
